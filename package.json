{
  "name": "proyecto",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "posters:thumbs": "node scripts/generate-poster-thumbnails.mjs",
    "dev": "npm run posters:thumbs && astro dev",
    "build": "npm run posters:thumbs && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.0.6",
    "firebase": "^12.16.0",
    "sharp": "^0.35.3"
  }
}
