---
import BaseLayout from '../../../layouts/Layout.astro';
import PortalSidebar from '../../../components/PortalSidebar.astro';
import AdministracionNav from '../../../components/AdministracionNav.astro';
---

<BaseLayout title="Administración de ensaios v2" lang="gl">
  <div class="private-layout">
    <PortalSidebar active="administracion" logoutId="admin-ensaios-v2-logout" />
    <main class="admin-main">
      <header class="private-page-header">
        <div>
          <span class="section-kicker">Administración</span>
          <h1>Administración de ensaios</h1>
          <p>Alta, asistencia, obras, cambio de data, baixa e eliminación.</p>
        </div>
        <div class="account-card">
          <span><small>Conta identificada</small><strong id="user-email">—</strong></span>
          <span><small>Nivel de acceso</small><strong id="user-level">Comprobando…</strong></span>
        </div>
      </header>

      <AdministracionNav active="ensaios" />

      <section id="loading" class="state-box">Comprobando permisos e cargando ensaios…</section>
      <section id="error-state" class="state-box error-box" hidden>
        <strong>Non foi posible abrir a administración de ensaios</strong>
        <p id="error-message"></p>
        <button id="retry" type="button" class="secondary">Tentar de novo</button>
      </section>

      <section id="app" hidden>
        <div class="toolbar">
          <label><span>Mostrar</span><select id="filter"><option value="activos">Ensaios activos</option><option value="todos">Todos</option><option value="baixa">Dados de baixa</option></select></label>
          <p id="count"></p>
          <div class="toolbar-actions">
            <a href="/portal/administracion/ensaios-analise-v2/" class="secondary action-link">Análise de asistencia</a>
            <button id="new-rehearsal" type="button" class="primary">Alta de ensaio</button>
          </div>
        </div>
        <div id="list" class="rehearsal-list"></div>
      </section>
    </main>
  </div>

  <dialog id="new-dialog" class="wide-dialog"><form id="new-form">
    <header><h2>Alta de ensaio</h2><button type="button" data-close="new-dialog">×</button></header>
    <div class="form-grid">
      <label><span>Data *</span><input name="data" type="date" required /></label>
      <label><span>Hora de inicio *</span><input name="horaInicio" type="time" required value="20:00" /></label>
      <label><span>Hora de fin</span><input name="horaFin" type="time" /></label>
      <label><span>Tipo de ensaio *</span><input name="tipoEnsaio" required value="Ensaio ordinario" /></label>
      <label class="span-2"><span>Lugar</span><input name="lugar" value="Centro Social Sur - Luís Braille 40" /></label>
      <label class="span-2"><span>Concerto relacionado</span><input name="concerto" /></label>
      <label class="span-2"><span>Descrición</span><textarea name="descricion" rows="2"></textarea></label>
      <label class="span-2"><span>Observacións</span><textarea name="observacions" rows="2"></textarea></label>
    </div>
    <p id="new-status" class="dialog-status"></p>
    <footer><button type="button" class="secondary" data-close="new-dialog">Cancelar</button><button type="submit" class="primary">Crear ensaio</button></footer>
  </form></dialog>

  <dialog id="manage-dialog" class="manage-dialog"><section class="manage-shell">
    <header><div><small>Xestión integral</small><h2 id="manage-title">Ensaio</h2></div><button type="button" data-close="manage-dialog">×</button></header>
    <nav class="manage-tabs">
      <button type="button" class="is-active" data-panel="attendance">Asistencia</button>
      <button type="button" data-panel="works">Obras</button>
    </nav>

    <section id="panel-attendance" class="manage-panel">
      <div class="attendance-summary"><strong id="attendance-summary">0 rexistros</strong><span>Marca a situación de cada coralista e garda os cambios.</span></div>
      <div id="people-list"></div>
      <div class="panel-footer"><p id="attendance-status" class="dialog-status"></p><button id="save-attendance" type="button" class="primary">Gardar asistencia</button></div>
    </section>

    <section id="panel-works" class="manage-panel" hidden>
      <div class="works-tools">
        <article class="work-tool">
          <small>Repertorio xeral</small>
          <h3>Engadir unha obra</h3>
          <label><span>Obra</span><select id="work-select"><option value="">Cargando repertorio…</option></select></label>
          <button id="add-work" type="button" class="secondary">Engadir desde Repertorio</button>
        </article>
        <article class="work-tool">
          <small>Programa de concerto</small>
          <h3>Cargar programa completo</h3>
          <label><span>Concerto</span><select id="concert-select"><option value="">Cargando concertos…</option></select></label>
          <button id="import-concert" type="button" class="secondary">Cargar programa do concerto</button>
        </article>
      </div>
      <div class="current-works-head"><div><small>Planificación actual</small><h3>Obras do ensaio</h3></div><strong id="works-count">0 obras</strong></div>
      <div id="works-list" class="works-list"></div>
      <p id="works-status" class="dialog-status"></p>
    </section>
    <footer class="manage-accept"><button id="accept-management" type="button" class="primary">Aceptar</button></footer>
  </section></dialog>

  <dialog id="date-dialog"><form id="date-form"><header><h2>Cambiar data</h2><button type="button" data-close="date-dialog">×</button></header><p id="date-name"></p><label><span>Nova data</span><input id="new-date" type="date" required /></label><p id="date-status" class="dialog-status"></p><footer><button type="button" class="secondary" data-close="date-dialog">Cancelar</button><button type="submit" class="primary">Gardar nova data</button></footer></form></dialog>
  <dialog id="cancel-dialog"><form id="cancel-form"><header><h2>Dar de baixa</h2><button type="button" data-close="cancel-dialog">×</button></header><p id="cancel-name"></p><p>O ensaio quedará marcado como cancelado e conservará as relacións existentes.</p><p id="cancel-status" class="dialog-status"></p><footer><button type="button" class="secondary" data-close="cancel-dialog">Cancelar</button><button type="submit" class="danger">Dar de baixa</button></footer></form></dialog>
  <dialog id="delete-dialog"><form id="delete-form"><header><h2>Eliminar ensaio</h2><button type="button" data-close="delete-dialog">×</button></header><p id="delete-name"></p><p><strong>Esta acción non se pode desfacer.</strong> Eliminarase tamén a información asociada segundo as regras do backend.</p><label><span>Escribe ELIMINAR para confirmar</span><input id="delete-confirm" autocomplete="off" /></label><p id="delete-status" class="dialog-status"></p><footer><button type="button" class="secondary" data-close="delete-dialog">Cancelar</button><button id="delete-submit" type="submit" class="danger-solid" disabled>Eliminar definitivamente</button></footer></form></dialog>
</BaseLayout>

<script>
import { auth, closePortalSession, waitForPortalUser } from '../../../lib/portal-session.js';

const $=(s)=>document.querySelector(s);
const loading=$('#loading'),errorState=$('#error-state'),errorMessage=$('#error-message'),app=$('#app'),list=$('#list'),filter=$('#filter'),count=$('#count'),userEmail=$('#user-email'),userLevel=$('#user-level');
const newDialog=$('#new-dialog'),manageDialog=$('#manage-dialog'),dateDialog=$('#date-dialog'),cancelDialog=$('#cancel-dialog'),deleteDialog=$('#delete-dialog');
let ensaios=[],selected=null,xestion={persoas:[],obras:[],repertorio:[],concertos:[],obrasCargadas:false};
const esc=(v='')=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const fmt=(v)=>{if(!v)return'Sen data';const d=new Date(`${String(v).slice(0,10)}T12:00:00`);return Number.isNaN(d.getTime())?String(v):new Intl.DateTimeFormat('gl-ES',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(d)};
const personName=(p)=>[p.primeiroApelido,p.segundoApelido,p.nome].filter(Boolean).join(' ')||p.nomeCompleto||'';

async function request(accion,extra={}){const user=auth.currentUser;if(!user?.email)throw new Error('A sesión non está activa.');const idToken=await user.getIdToken();const response=await fetch('/api/ensaios-admin-v2',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({idToken,accion,...extra})});const result=await response.json().catch(()=>null);if(!response.ok||!result?.ok)throw new Error(result?.erro||`Erro HTTP ${response.status}`);return result;}
async function manageRequest(accion,extra={}){const user=auth.currentUser;if(!user?.email)throw new Error('A sesión non está activa.');const idToken=await user.getIdToken();const response=await fetch('/api/ensaios-admin-v2-xestion',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({idToken,accion,idEnsaio:selected?.idEnsaio,...extra})});const result=await response.json().catch(()=>null);if(!response.ok||!result?.ok)throw new Error(result?.erro||`Erro HTTP ${response.status}`);return result;}
async function draftRequest(accion,extra={}){const user=auth.currentUser;if(!user?.email)throw new Error('A sesión non está activa.');const idToken=await user.getIdToken();const response=await fetch('/api/ensaios-borrador',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({idToken,accion,idEnsaio:selected?.idEnsaio,...extra})});const result=await response.json().catch(()=>null);if(!response.ok||!result?.ok)throw new Error(result?.erro||`Erro HTTP ${response.status}`);return result;}
function visible(){const mode=filter instanceof HTMLSelectElement?filter.value:'activos';return ensaios.filter(i=>mode==='todos'||(mode==='baixa'?i.cancelado:!i.cancelado));}

function draw(){if(!(list instanceof HTMLElement))return;const rows=visible();if(count instanceof HTMLElement)count.textContent=`${rows.length} ensaio${rows.length===1?'':'s'}`;if(!rows.length){list.innerHTML='<section class="state-box">Non hai ensaios para este filtro.</section>';return;}list.innerHTML=rows.map(i=>`<article class="rehearsal-card ${i.cancelado?'is-cancelled':''}">
  <div class="date-cell"><small>Data</small><strong>${esc(fmt(i.data))}</strong></div>
  <div class="info-cell"><h2>${esc(i.tipoEnsaio||'Ensaio')}</h2><p>${esc([i.horaInicio,i.lugar,i.concertoNome||i.concerto].filter(Boolean).join(' · '))}</p><div class="meta"><span><small>Obras</small><strong>${Number(i.obras||0)}</strong></span><span><small>Asistencias</small><strong>${Number(i.asistencias||0)}</strong></span></div></div>
  <div class="state-cell"><small>Estado</small><strong>${i.cancelado?'Baixa':'Activo'}</strong></div>
  <div class="actions"><button type="button" class="primary manage-rehearsal" data-id="${esc(i.idEnsaio)}">Xestionar</button><button type="button" class="secondary change-date" data-id="${esc(i.idEnsaio)}">Cambiar data</button>${i.cancelado?'':`<button type="button" class="secondary cancel-rehearsal" data-id="${esc(i.idEnsaio)}">Dar de baixa</button>`}<button type="button" class="danger delete-rehearsal" data-id="${esc(i.idEnsaio)}">Eliminar</button></div>
</article>`).join('');}

async function load(){if(loading instanceof HTMLElement)loading.hidden=false;if(errorState instanceof HTMLElement)errorState.hidden=true;if(app instanceof HTMLElement)app.hidden=true;try{const user=await waitForPortalUser();if(!user?.email)throw new Error('Non hai unha sesión válida.');if(userEmail instanceof HTMLElement)userEmail.textContent=user.email;const result=await request('listar');ensaios=Array.isArray(result.ensaios)?result.ensaios:[];if(userLevel instanceof HTMLElement)userLevel.textContent=result.nivel||'Administración';if(loading instanceof HTMLElement)loading.hidden=true;if(app instanceof HTMLElement)app.hidden=false;draw();}catch(error){if(loading instanceof HTMLElement)loading.hidden=true;if(errorState instanceof HTMLElement)errorState.hidden=false;if(errorMessage instanceof HTMLElement)errorMessage.textContent=error instanceof Error?error.message:'Produciuse un erro inesperado.';}}

function pintarPersoas(){const box=$('#people-list'),summary=$('#attendance-summary');if(!(box instanceof HTMLElement))return;const voces=['Soprano','Contralto','Tenor','Baixo'];const marcadas=xestion.persoas.filter(p=>p.estado).length;if(summary instanceof HTMLElement)summary.textContent=`${marcadas} de ${xestion.persoas.length} marcados`;box.innerHTML=voces.map(v=>{const ps=xestion.persoas.filter(p=>String(p.voz).toLowerCase()===v.toLowerCase()).sort((a,b)=>personName(a).localeCompare(personName(b),'gl'));if(!ps.length)return'';return`<section class="voice-group"><h3>${v}<span>${ps.length}</span></h3>${ps.map(p=>`<article class="attendance-person" data-person="${esc(p.id)}"><strong>${esc(personName(p))}</strong><div class="attendance-actions"><button type="button" data-attendance="asiste" class="${p.estado==='asiste'?'is-selected':''}">Asiste</button><button type="button" data-attendance="non_asiste" class="${p.estado==='non_asiste'?'is-selected is-negative':''}">Non asiste</button><button type="button" data-attendance="xustificada" class="${p.estado==='xustificada'?'is-selected is-justified':''}">Non asiste con xustificación</button></div><label class="justification" ${p.estado==='xustificada'?'':'hidden'}><span>Xustificación</span><input data-justification value="${esc(p.xustificacion||'')}" placeholder="Indica o motivo" /></label></article>`).join('')}</section>`}).join('');}

function pintarObras(){const list=$('#works-list'),count=$('#works-count'),workSelect=$('#work-select'),concertSelect=$('#concert-select');if(count instanceof HTMLElement)count.textContent=`${xestion.obras.length} obra${xestion.obras.length===1?'':'s'}`;if(list instanceof HTMLElement)list.innerHTML=xestion.obras.length?xestion.obras.map((o,n)=>`<article class="work-row"><span class="work-order">${n+1}</span><div><strong>${esc(o.nomeObra)}</strong><small>${esc(o.compositor||'')}</small></div>${o.tipoTraballo?`<span class="work-type">${esc(o.tipoTraballo)}</span>`:''}</article>`).join(''):'<div class="empty-panel">Aínda non hai obras vinculadas a este ensaio.</div>';if(workSelect instanceof HTMLSelectElement)workSelect.innerHTML='<option value="">Selecciona unha obra</option>'+xestion.repertorio.map(o=>`<option value="${esc(o.idRepertorio)}">${esc(o.nomeObra)}${o.compositor?' · '+esc(o.compositor):''}</option>`).join('');if(concertSelect instanceof HTMLSelectElement)concertSelect.innerHTML='<option value="">Selecciona un concerto</option>'+xestion.concertos.sort((a,b)=>String(b.data).localeCompare(String(a.data))).map(c=>`<option value="${esc(c.idConcerto)}">${esc(c.data||'')} · ${esc(c.nome)} · ${Number(c.obras ?? (c.repertorio||[]).length)} obras</option>`).join('');}
function aplicarDraftObras(draft){const catalogo=new Map(xestion.repertorio.map(o=>[String(o.idRepertorio),o]));xestion.obras=(Array.isArray(draft?.repertorio)?draft.repertorio:[]).map((row,index)=>{const id=String(row.repertorio||row.idRepertorio||'');const obra=catalogo.get(id)||{};return{idRepertorio:id,nomeObra:obra.nomeObra||id,compositor:obra.compositor||'',orde:Number(row.orde)||index+1,tipoTraballo:row.tipoTraballo||'',observacions:row.observacions||''};}).filter(o=>o.idRepertorio).sort((a,b)=>(a.orde||9999)-(b.orde||9999));xestion.obrasCargadas=true;pintarObras();}

async function cargarObras(){if(xestion.obrasCargadas){pintarObras();return;}const status=$('#works-status');if(status instanceof HTMLElement)status.textContent='Cargando repertorio e programas…';try{const result=await manageRequest('obterObras');xestion.obras=Array.isArray(result.obras)?result.obras:[];xestion.repertorio=Array.isArray(result.repertorio)?result.repertorio:[];xestion.concertos=Array.isArray(result.concertos)?result.concertos:[];xestion.obrasCargadas=true;pintarObras();if(status instanceof HTMLElement)status.textContent='';}catch(error){if(status instanceof HTMLElement)status.textContent=error instanceof Error?error.message:'Non foi posible cargar as obras.';}}

function activarPanel(panel){document.querySelectorAll('.manage-tabs [data-panel]').forEach(btn=>btn.classList.toggle('is-active',btn.getAttribute('data-panel')===panel));const attendance=$('#panel-attendance'),works=$('#panel-works');if(attendance instanceof HTMLElement)attendance.hidden=panel!=='attendance';if(works instanceof HTMLElement)works.hidden=panel!=='works';if(panel==='works')cargarObras();}

async function abrirXestion(item){selected=item;xestion={persoas:[],obras:[],repertorio:[],concertos:[],obrasCargadas:false};if(!(manageDialog instanceof HTMLDialogElement))return;const title=$('#manage-title'),status=$('#attendance-status');if(title instanceof HTMLElement)title.textContent=`${item.tipoEnsaio} · ${fmt(item.data)}`;activarPanel('attendance');if(status instanceof HTMLElement)status.textContent='Cargando asistencia e obras…';manageDialog.showModal();try{const [result,draftResult]=await Promise.all([manageRequest('obterXestion'),draftRequest('reiniciar')]);xestion.persoas=Array.isArray(result.persoas)?result.persoas:[];xestion.obras=Array.isArray(result.obras)?result.obras:[];xestion.repertorio=Array.isArray(result.repertorio)?result.repertorio:[];xestion.concertos=Array.isArray(result.concertos)?result.concertos:[];xestion.obrasCargadas=true;pintarPersoas();aplicarDraftObras(draftResult.draft);if(status instanceof HTMLElement)status.textContent='';}catch(error){if(status instanceof HTMLElement)status.textContent=error instanceof Error?error.message:'Non foi posible cargar a xestión do ensaio.';}}
function closeDialog(id){const el=document.getElementById(id);if(el instanceof HTMLDialogElement)el.close();}

document.querySelectorAll('[data-close]').forEach(btn=>btn.addEventListener('click',()=>closeDialog(btn.getAttribute('data-close')||'')));
document.querySelectorAll('.manage-tabs [data-panel]').forEach(btn=>btn.addEventListener('click',()=>activarPanel(btn.getAttribute('data-panel')||'attendance')));
filter?.addEventListener('change',draw);$('#retry')?.addEventListener('click',load);$('#new-rehearsal')?.addEventListener('click',()=>{const form=$('#new-form');if(form instanceof HTMLFormElement)form.reset();const start=form?.elements.namedItem('horaInicio');if(start instanceof HTMLInputElement)start.value='20:00';const type=form?.elements.namedItem('tipoEnsaio');if(type instanceof HTMLInputElement)type.value='Ensaio ordinario';const place=form?.elements.namedItem('lugar');if(place instanceof HTMLInputElement)place.value='Centro Social Sur - Luís Braille 40';if(newDialog instanceof HTMLDialogElement)newDialog.showModal();});$('#admin-ensaios-v2-logout')?.addEventListener('click',async()=>{await closePortalSession();location.href='/';});

list?.addEventListener('click',(event)=>{const target=event.target;if(!(target instanceof HTMLButtonElement))return;selected=ensaios.find(i=>i.idEnsaio===target.dataset.id)||null;if(!selected)return;if(target.classList.contains('manage-rehearsal'))abrirXestion(selected);if(target.classList.contains('change-date')){const p=$('#date-name');if(p instanceof HTMLElement)p.textContent=`${selected.tipoEnsaio} · ${fmt(selected.data)}`;const input=$('#new-date');if(input instanceof HTMLInputElement)input.value=selected.data||'';if(dateDialog instanceof HTMLDialogElement)dateDialog.showModal();}if(target.classList.contains('cancel-rehearsal')){const p=$('#cancel-name');if(p instanceof HTMLElement)p.textContent=`${selected.tipoEnsaio} · ${fmt(selected.data)}`;if(cancelDialog instanceof HTMLDialogElement)cancelDialog.showModal();}if(target.classList.contains('delete-rehearsal')){const p=$('#delete-name');if(p instanceof HTMLElement)p.textContent=`${selected.tipoEnsaio} · ${fmt(selected.data)}`;const confirm=$('#delete-confirm'),submit=$('#delete-submit');if(confirm instanceof HTMLInputElement)confirm.value='';if(submit instanceof HTMLButtonElement)submit.disabled=true;if(deleteDialog instanceof HTMLDialogElement)deleteDialog.showModal();}});

$('#people-list')?.addEventListener('click',(event)=>{const target=event.target;if(!(target instanceof HTMLButtonElement)||!target.dataset.attendance)return;const row=target.closest('[data-person]');const id=row?.getAttribute('data-person');const p=xestion.persoas.find(x=>x.id===id);if(!p)return;p.estado=target.dataset.attendance;if(p.estado!=='xustificada')p.xustificacion='';pintarPersoas();});
$('#people-list')?.addEventListener('input',(event)=>{const target=event.target;if(!(target instanceof HTMLInputElement)||!target.matches('[data-justification]'))return;const row=target.closest('[data-person]'),id=row?.getAttribute('data-person'),p=xestion.persoas.find(x=>x.id===id);if(p)p.xustificacion=target.value;});
$('#save-attendance')?.addEventListener('click',async()=>{const status=$('#attendance-status');const persoas=xestion.persoas.filter(p=>p.estado);const missing=persoas.find(p=>p.estado==='xustificada'&&!String(p.xustificacion||'').trim());if(missing){if(status instanceof HTMLElement)status.textContent=`Falta a xustificación de ${personName(missing)}.`;return;}try{if(status instanceof HTMLElement)status.textContent='Preparando asistencia…';const result=await draftRequest('gardarAsistencias',{persoas});if(status instanceof HTMLElement)status.textContent=`${result.gardadas||0} rexistros preparados. Pulsa Aceptar para confirmar.`;}catch(error){if(status instanceof HTMLElement)status.textContent=error instanceof Error?error.message:'Non foi posible preparar a asistencia.';}});

$('#add-work')?.addEventListener('click',async()=>{const select=$('#work-select'),status=$('#works-status');if(!(select instanceof HTMLSelectElement)||!select.value)return;try{if(status instanceof HTMLElement)status.textContent='Engadindo obra…';const result=await draftRequest('gardarObra',{idRepertorio:select.value});aplicarDraftObras(result.draft);if(status instanceof HTMLElement)status.textContent='Obra preparada. Pulsa Aceptar para confirmar.';}catch(error){if(status instanceof HTMLElement)status.textContent=error instanceof Error?error.message:'Non foi posible engadir a obra.';}});
$('#import-concert')?.addEventListener('click',async()=>{const select=$('#concert-select'),status=$('#works-status');if(!(select instanceof HTMLSelectElement)||!select.value)return;try{if(status instanceof HTMLElement)status.textContent='Lendo o programa real do concerto…';const result=await draftRequest('incluírProgramaConcerto',{idConcerto:select.value});aplicarDraftObras(result.draft);if(status instanceof HTMLElement)status.textContent=`Programa preparado: ${result.engadidas||0} obras novas de ${result.totalPrograma||0}. Pulsa Aceptar para confirmar.`;}catch(error){if(status instanceof HTMLElement)status.textContent=error instanceof Error?error.message:'Non foi posible preparar o programa.';}});
$('#accept-management')?.addEventListener('click',async()=>{const button=$('#accept-management'),attendanceStatus=$('#attendance-status'),worksStatus=$('#works-status');const persoas=xestion.persoas.filter(p=>p.estado);const missing=persoas.find(p=>p.estado==='xustificada'&&!String(p.xustificacion||'').trim());if(missing){if(attendanceStatus instanceof HTMLElement)attendanceStatus.textContent=`Falta a xustificación de ${personName(missing)}.`;return;}try{if(button instanceof HTMLButtonElement)button.disabled=true;if(attendanceStatus instanceof HTMLElement)attendanceStatus.textContent='Preparando e confirmando asistencia…';if(worksStatus instanceof HTMLElement)worksStatus.textContent='Confirmando cambios en Sheet e R2…';if(persoas.length)await draftRequest('gardarAsistencias',{persoas});await draftRequest('finalizar');closeDialog('manage-dialog');await load();}catch(error){const message=error instanceof Error?error.message:'Non foi posible confirmar os cambios.';if(attendanceStatus instanceof HTMLElement)attendanceStatus.textContent=message;if(worksStatus instanceof HTMLElement)worksStatus.textContent=message;}finally{if(button instanceof HTMLButtonElement)button.disabled=false;}});

$('#new-form')?.addEventListener('submit',async(event)=>{event.preventDefault();const form=event.currentTarget;if(!(form instanceof HTMLFormElement))return;const status=$('#new-status'),data=Object.fromEntries(new FormData(form).entries());try{if(status instanceof HTMLElement)status.textContent='Creando…';await request('crear',data);closeDialog('new-dialog');await load();}catch(error){if(status instanceof HTMLElement)status.textContent=error instanceof Error?error.message:'Non foi posible crear o ensaio.';}});
$('#date-form')?.addEventListener('submit',async(event)=>{event.preventDefault();const input=$('#new-date'),status=$('#date-status');if(!selected||!(input instanceof HTMLInputElement)||!input.value)return;try{if(status instanceof HTMLElement)status.textContent='Gardando…';await request('cambiarData',{idEnsaio:selected.idEnsaio,data:input.value});closeDialog('date-dialog');await load();}catch(error){if(status instanceof HTMLElement)status.textContent=error instanceof Error?error.message:'Non foi posible cambiar a data.';}});
$('#cancel-form')?.addEventListener('submit',async(event)=>{event.preventDefault();const status=$('#cancel-status');if(!selected)return;try{if(status instanceof HTMLElement)status.textContent='Gardando…';await request('darBaixa',{idEnsaio:selected.idEnsaio});closeDialog('cancel-dialog');await load();}catch(error){if(status instanceof HTMLElement)status.textContent=error instanceof Error?error.message:'Non foi posible dar de baixa o ensaio.';}});
$('#delete-confirm')?.addEventListener('input',(event)=>{const target=event.target,submit=$('#delete-submit');if(target instanceof HTMLInputElement&&submit instanceof HTMLButtonElement)submit.disabled=target.value.trim()!=='ELIMINAR';});
$('#delete-form')?.addEventListener('submit',async(event)=>{event.preventDefault();const status=$('#delete-status');if(!selected)return;try{if(status instanceof HTMLElement)status.textContent='Eliminando…';await request('eliminar',{idEnsaio:selected.idEnsaio});closeDialog('delete-dialog');await load();}catch(error){if(status instanceof HTMLElement)status.textContent=error instanceof Error?error.message:'Non foi posible eliminar o ensaio.';}});
load();
</script>

<style is:global>
.admin-main{padding:2rem;min-width:0}.private-page-header{display:flex;justify-content:space-between;gap:2rem;align-items:flex-start;margin-bottom:1.5rem}.private-page-header h1{font-size:clamp(2rem,4vw,3.2rem);margin:.25rem 0}.section-kicker{color:#b58a3b;font-weight:700;text-transform:uppercase;letter-spacing:.08em}.account-card,.state-box,.toolbar,.rehearsal-card{border:1px solid #ddd5d1;background:#fff}.account-card{min-width:250px;padding:1rem;display:grid;gap:1rem}.account-card span{display:grid;gap:.25rem}.account-card small,.date-cell small,.state-cell small,.meta small,.work-tool>small,.current-works-head small{font-size:.72rem;text-transform:uppercase;letter-spacing:.04em;color:#756d69}.toolbar{display:flex;align-items:end;gap:1rem;padding:1rem;margin:1.5rem 0}.toolbar label{display:grid;gap:.35rem}.toolbar p{margin:0 auto 0 0}.toolbar-actions{display:flex;gap:.6rem;align-items:center}.action-link{text-decoration:none;display:inline-flex;align-items:center}.rehearsal-list{display:grid;gap:1rem}.rehearsal-card{display:grid;grid-template-columns:220px 1fr 110px auto;gap:1rem;align-items:center;padding:1rem}.date-cell,.state-cell{display:grid;gap:.35rem}.date-cell strong{line-height:1.35}.info-cell h2{margin:0 0 .4rem;color:#74162f}.info-cell p{margin:0;color:#6f6865}.meta{display:flex;gap:1.5rem;margin-top:.7rem}.meta span{display:grid;gap:.15rem}.meta strong{font-size:1.05rem}.actions{display:flex;gap:.5rem;flex-wrap:wrap;justify-content:flex-end}.is-cancelled{opacity:.72}.state-box{padding:1.25rem}.error-box{color:#6e2433}button,.secondary{padding:.75rem 1rem;border:1px solid #cfc3bf;background:#fff;color:#3f3431;cursor:pointer;font:inherit}.primary{background:#74162f;color:#fff;border-color:#74162f}.danger,.danger-solid{color:#74162f;border-color:#74162f}.danger-solid{background:#74162f;color:#fff}dialog{width:min(92vw,620px);border:1px solid #d7ceca;padding:0}dialog form{padding:1.25rem}dialog header,dialog footer{display:flex;justify-content:space-between;align-items:center;gap:1rem}dialog footer{justify-content:flex-end;margin-top:1.25rem}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:1rem}.form-grid label,label{display:grid;gap:.35rem}.span-2{grid-column:1/-1}input,select,textarea{padding:.75rem;border:1px solid #d4cbc7;font:inherit}.dialog-status{min-height:1.25rem;color:#74162f}
.manage-dialog{width:min(92vw,1180px);max-height:84vh}.manage-shell{padding:1.2rem}.manage-shell>header{display:flex;justify-content:space-between;align-items:flex-start;padding-bottom:1rem;border-bottom:1px solid #e5ddda}.manage-shell>header h2{margin:.25rem 0;color:#74162f}.manage-tabs{display:flex;gap:.5rem;padding:1rem 0;border-bottom:1px solid #e5ddda}.manage-tabs button.is-active{background:#74162f;color:#fff;border-color:#74162f}.manage-panel{padding-top:1rem}.attendance-summary{display:flex;justify-content:space-between;gap:1rem;margin-bottom:1rem;color:#756d69}.voice-group{margin-bottom:1rem}.voice-group h3{display:flex;justify-content:space-between;color:#74162f;border-bottom:1px solid #e7dfdc;padding:.6rem 0}.attendance-person{display:grid;grid-template-columns:minmax(220px,1fr) auto;gap:.8rem;align-items:center;padding:.7rem 0;border-bottom:1px solid #eee8e5}.attendance-actions{display:flex;gap:.4rem;flex-wrap:wrap}.attendance-actions button{padding:.55rem .75rem}.attendance-actions .is-selected{background:#e7f2ea;border-color:#578265}.attendance-actions .is-negative{background:#f6e7e9;border-color:#9c5964}.attendance-actions .is-justified{background:#f7f0dd;border-color:#b58a3b}.justification{grid-column:1/-1}.panel-footer{display:flex;justify-content:space-between;align-items:center;gap:1rem;position:sticky;bottom:0;background:#fff;padding:1rem 0 .2rem;border-top:1px solid #e5ddda}.works-tools{display:grid;grid-template-columns:1fr 1fr;gap:1rem}.work-tool{border:1px solid #e2dad6;padding:1rem;display:grid;gap:.8rem}.work-tool h3{margin:0;color:#74162f}.work-tool button{justify-self:start}.current-works-head{display:flex;justify-content:space-between;align-items:end;margin:1.3rem 0 .6rem}.current-works-head h3{margin:.15rem 0}.works-list{border-top:1px solid #e7dfdc}.work-row{display:grid;grid-template-columns:42px 1fr auto;gap:.8rem;align-items:center;padding:.75rem 0;border-bottom:1px solid #eee8e5}.work-order{width:30px;height:30px;border:1px solid #d8cfcb;display:grid;place-items:center}.work-row>div{display:grid;gap:.2rem}.work-row small{color:#756d69}.work-type{font-size:.85rem;color:#756d69}.empty-panel{padding:1.2rem 0;color:#756d69}.manage-accept{display:flex;justify-content:flex-end;position:sticky;bottom:0;background:#fff;padding:1rem 0 .2rem;margin-top:1rem;border-top:1px solid #e5ddda}
@media(max-width:1000px){.rehearsal-card{grid-template-columns:180px 1fr}.state-cell{grid-column:1}.actions{grid-column:2;justify-content:flex-start}.works-tools{grid-template-columns:1fr}}
@media(max-width:700px){.admin-main{padding:1rem}.private-page-header{display:block}.account-card{margin-top:1rem;min-width:0}.toolbar{align-items:stretch;flex-direction:column}.toolbar-actions{display:grid}.rehearsal-card{grid-template-columns:1fr}.state-cell,.actions{grid-column:auto}.actions{justify-content:flex-start}.form-grid{grid-template-columns:1fr}.span-2{grid-column:auto}.manage-dialog{width:100vw;max-width:none;max-height:92vh}.attendance-summary{display:grid}.attendance-person{grid-template-columns:1fr}.attendance-actions{display:grid;grid-template-columns:1fr}.justification{grid-column:auto}.panel-footer{display:grid}.manage-tabs button{flex:1}.work-row{grid-template-columns:34px 1fr}.work-type{grid-column:2}.manage-accept button{width:100%}}
</style>