---
const idFoto = Astro.url.searchParams.get('idFoto');
const destino = idFoto
  ? `/portal/administracion/fotografias/?idFoto=${encodeURIComponent(idFoto)}`
  : '/portal/administracion/fotografias/';
return Astro.redirect(destino, 302);
---
