---
const { active = '' } = Astro.props;

const items = [
  { id: 'persoas', label: 'Persoas', path: '/portal/administracion/persoas/' },
  { id: 'ensaios', label: 'Ensaios', path: '/portal/administracion/ensaios/' },
  { id: 'concertos', label: 'Concertos', path: '/portal/administracion/concertos/' },
  { id: 'fotografias', label: 'Fotografías', path: '/portal/administracion/fotografias/' },
  { id: 'repertorio', label: 'Repertorio', path: '/portal/administracion/repertorio/' },
  { id: 'arquivo', label: 'Arquivo e préstamos', path: '/portal/administracion/arquivo/' },
  { id: 'doazons', label: 'Doazóns', path: '/portal/administracion/doazons/' },
  { id: 'auditoria', label: 'Auditoría', path: '/portal/administracion/auditoria/' },
  { id: 'estado', label: 'Estado do sistema', path: '/portal/administracion/estado/' }
];
---

<nav class="admin-nav" aria-label="Administración">
  {items.map((item) => (
    <a
      href={item.path}
      class:list={['admin-nav-link', { 'is-active': active === item.id }]}
      aria-current={active === item.id ? 'page' : undefined}
    >
      {item.label}
    </a>
  ))}
</nav>

<script is:inline>
  (() => {
    if (!window.location.pathname.startsWith('/portal/administracion/concertos')) return;
    if (window.__scppConcertosAdminSheetProbas) return;
    window.__scppConcertosAdminSheetProbas = true;

    const fetchOriginal = window.fetch.bind(window);
    window.fetch = (input, init) => {
      try {
        const valor = typeof input === 'string' ? input : input instanceof Request ? input.url : String(input || '');
        const url = new URL(valor, window.location.href);
        if (url.pathname !== '/api/concertos-admin') return fetchOriginal(input, init);

        const body = typeof init?.body === 'string' ? JSON.parse(init.body) : null;
        if (body?.accion !== 'listar') return fetchOriginal(input, init);

        url.pathname = '/api/concertos-admin-list';
        const destino = typeof input === 'string' ? url.pathname + url.search : new Request(url.toString(), input);
        return fetchOriginal(destino, init);
      } catch {
        return fetchOriginal(input, init);
      }
    };
  })();
</script>

<style is:global>
  body.portal-private-body {
    background: #f5f3f1;
  }

  body.portal-private-body .private-layout {
    display: flex;
    width: 100%;
    min-height: 100vh;
  }

  body.portal-private-body .private-layout > .private-sidebar {
    flex: 0 0 282px;
  }

  body.portal-private-body .private-layout > .status-main {
    flex: 1 1 auto;
    width: auto;
    min-width: 0;
  }

  .admin-nav {
    display: flex;
    flex-wrap: wrap;
    gap: .55rem;
    margin: 0 0 1.5rem;
  }

  .admin-nav-link {
    display: inline-flex;
    align-items: center;
    min-height: 2.35rem;
    padding: .55rem .9rem;
    border: 1px solid rgba(93, 20, 43, .2);
    border-radius: 0;
    background: #fff;
    color: #3b2028;
    font-size: .9rem;
    font-weight: 700;
    text-decoration: none;
    transition: border-color .15s ease, background .15s ease;
  }

  .admin-nav-link:hover {
    border-color: rgba(93, 20, 43, .5);
  }

  .admin-nav-link.is-active {
    background: #5d142b;
    border-color: #5d142b;
    color: #fff;
  }

  /* Estado do sistema: estas regras son globais porque as tarxetas créanse con JavaScript. */
  body.portal-private-body .status-main {
    padding: clamp(1.6rem, 3vw, 3rem) clamp(1.4rem, 3vw, 3.5rem) 4rem;
  }

  body.portal-private-body .status-main .private-page-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 2rem;
    margin-bottom: 1.8rem;
    padding-bottom: 1.35rem;
    border-bottom: 1px solid #ded8d3;
  }

  body.portal-private-body .status-main .private-page-header > div:first-child {
    min-width: 0;
  }

  body.portal-private-body .status-main .private-page-header h1 {
    margin: .25rem 0 .55rem;
  }

  body.portal-private-body .status-main .private-page-header p {
    max-width: 760px;
    margin: 0;
    color: #625c58;
  }

  body.portal-private-body .status-main .account-card {
    display: grid;
    flex: 0 0 min(310px, 32vw);
    gap: .85rem;
    min-width: 260px;
    padding: 1rem 1.15rem;
    border: 1px solid #ded8d3;
    border-radius: 6px;
    background: #fff;
  }

  body.portal-private-body .status-main .account-card span {
    display: grid;
    gap: .22rem;
  }

  body.portal-private-body .status-main .account-card span + span {
    padding-top: .75rem;
    border-top: 1px solid #eee8e5;
  }

  body.portal-private-body .status-main .account-card small {
    color: #7c726d;
    font-size: .68rem;
    font-weight: 800;
    letter-spacing: .05em;
    text-transform: uppercase;
  }

  body.portal-private-body .status-main .account-card strong {
    overflow-wrap: anywhere;
    color: #34312f;
    font-size: .9rem;
  }

  body.portal-private-body .status-main .state-box,
  body.portal-private-body .status-main .overview-card {
    border: 1px solid #ded8d3;
    border-radius: 7px;
    background: #fff;
    box-shadow: none;
  }

  body.portal-private-body .status-main .state-box {
    padding: 1.2rem 1.3rem;
  }

  body.portal-private-body .status-main .overview-card {
    display: flex;
    align-items: stretch;
    justify-content: space-between;
    gap: 1.75rem;
    padding: 1.45rem 1.55rem;
    border-top: 4px solid #5d142b;
  }

  body.portal-private-body .status-main .overview-card > div:first-child {
    flex: 1 1 auto;
    min-width: 0;
  }

  body.portal-private-body .status-main .overview-kicker,
  body.portal-private-body .status-main .overview-meta span {
    display: block;
    margin-bottom: .35rem;
    color: #755e65;
    font-size: .72rem;
    font-weight: 800;
    letter-spacing: .07em;
    text-transform: uppercase;
  }

  body.portal-private-body .status-main .overall-state {
    display: block;
    margin-bottom: .55rem;
    font-size: clamp(1.65rem, 3vw, 2.35rem);
    line-height: 1.08;
  }

  body.portal-private-body .status-main .overview-card p {
    margin: 0;
    color: #68575d;
  }

  body.portal-private-body .status-main .overview-meta {
    display: grid;
    align-content: center;
    min-width: 220px;
    padding-left: 1.35rem;
    border-left: 1px solid #eee8e5;
    text-align: left;
  }

  body.portal-private-body .status-main .overview-meta strong,
  body.portal-private-body .status-main .overview-meta small {
    display: block;
  }

  body.portal-private-body .status-main .overview-meta small {
    margin-top: .35rem;
    color: #806f74;
  }

  body.portal-private-body .status-main .status-toolbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.25rem;
    margin: 1.35rem 0 1.6rem;
    padding: .95rem 1.05rem;
    border: 1px solid #e1dbd6;
    border-radius: 6px;
    background: #faf9f7;
  }

  body.portal-private-body .status-main .status-toolbar p {
    margin: 0;
    color: #6b5b60;
    font-size: .9rem;
    line-height: 1.5;
  }

  body.portal-private-body .status-main .checks-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.25rem;
  }

  body.portal-private-body .status-main .check-card {
    position: relative;
    display: flex;
    flex-direction: column;
    min-width: 0;
    min-height: 210px;
    overflow: hidden;
    padding: 1.25rem 1.3rem 1.2rem;
    border: 1px solid #dcd6d1;
    border-radius: 7px;
    background: #fff;
    box-shadow: 0 5px 18px rgba(54,42,45,.045);
  }

  body.portal-private-body .status-main .check-card::before {
    content: '';
    position: absolute;
    inset: 0 auto 0 0;
    width: 5px;
    background: #777;
  }

  body.portal-private-body .status-main .check-card.is-ok::before { background: #4b7c5d; }
  body.portal-private-body .status-main .check-card.is-warning::before,
  body.portal-private-body .status-main .check-card.is-running::before { background: #a37a24; }
  body.portal-private-body .status-main .check-card.is-error::before { background: #9a3944; }

  body.portal-private-body .status-main .check-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    padding-bottom: .9rem;
    border-bottom: 1px solid #eee8e5;
  }

  body.portal-private-body .status-main .check-head h2 {
    margin: 0;
    color: #4e2833;
    font-size: 1.02rem;
    line-height: 1.35;
  }

  body.portal-private-body .status-main .check-badge {
    flex: 0 0 auto;
    padding: .34rem .62rem;
    border: 1px solid #e4dcdf;
    border-radius: 999px;
    background: #f7f3f4;
    color: #49383e;
    font-size: .72rem;
    font-weight: 800;
    line-height: 1.2;
  }

  body.portal-private-body .status-main .check-card dl {
    display: grid;
    gap: 0;
    margin: .85rem 0 1rem;
  }

  body.portal-private-body .status-main .check-card dl div {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: baseline;
    gap: 1rem;
    padding: .62rem 0;
    border-bottom: 1px solid #f0ebe8;
  }

  body.portal-private-body .status-main .check-card dl div:last-child {
    border-bottom: 0;
  }

  body.portal-private-body .status-main .check-card dt {
    color: #7a686e;
    font-size: .8rem;
  }

  body.portal-private-body .status-main .check-card dd {
    margin: 0;
    color: #3e3638;
    font-size: .84rem;
    font-weight: 700;
    text-align: right;
    overflow-wrap: anywhere;
  }

  body.portal-private-body .status-main .check-card a {
    display: inline-flex;
    width: fit-content;
    margin-top: auto;
    padding-top: .25rem;
    color: #5d142b;
    font-size: .84rem;
    font-weight: 800;
    text-decoration: none;
  }

  body.portal-private-body .status-main .check-card a:hover {
    text-decoration: underline;
  }

  body.portal-private-body .status-main .check-error {
    margin: 0 0 .9rem;
    padding: .65rem .75rem;
    border: 1px solid #ead3d6;
    border-radius: 4px;
    background: #fff8f8;
    color: #8b2d36;
    font-size: .8rem;
    overflow-wrap: anywhere;
  }

  @media (max-width: 980px) {
    body.portal-private-body .status-main .checks-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 900px) {
    body.portal-private-body .private-layout {
      display: block;
    }

    body.portal-private-body .private-layout > .private-sidebar,
    body.portal-private-body .private-layout > .status-main {
      width: 100%;
    }

    body.portal-private-body .status-main .private-page-header,
    body.portal-private-body .status-main .overview-card,
    body.portal-private-body .status-main .status-toolbar {
      display: grid;
    }

    body.portal-private-body .status-main .account-card {
      min-width: 0;
      width: 100%;
    }

    body.portal-private-body .status-main .overview-meta {
      min-width: 0;
      padding: 1rem 0 0;
      border-top: 1px solid #eee8e5;
      border-left: 0;
    }
  }

  @media (max-width: 620px) {
    body.portal-private-body .status-main {
      padding: 1.15rem .9rem 2.5rem;
    }

    body.portal-private-body .status-main .check-head {
      display: grid;
      gap: .65rem;
    }

    body.portal-private-body .status-main .check-badge {
      justify-self: start;
    }

    body.portal-private-body .status-main .check-card dl div {
      grid-template-columns: 1fr;
      gap: .25rem;
    }

    body.portal-private-body .status-main .check-card dd {
      text-align: left;
    }
  }
</style>


<style is:global>
  /* REPERTORIO_DYNAMIC_GLOBAL_STYLE
     Detalle e edición son xerados con JavaScript; estes estilos globais
     quedan limitados aos IDs exclusivos desta pantalla. */
  #edit-dialog {
    width: min(780px, calc(100vw - 2rem));
    padding: 0;
    border: 1px solid #d8d0ca;
    border-radius: 6px;
    background: #fff;
    box-shadow: 0 26px 80px rgb(0 0 0 / 22%);
  }
  #edit-dialog::backdrop { background: rgb(0 0 0 / 35%); }
  #edit-dialog form { display: grid; gap: 0; padding: 0; }
  #edit-dialog form > header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.2rem 1.3rem;
    border-bottom: 1px solid #e6dfda;
    background: #fff;
  }
  #edit-dialog form > header h2 { margin: .2rem 0; color: #6f1d2b; }
  #edit-dialog .edit-fields {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem 1.1rem;
    padding: 1.25rem 1.3rem;
    background: #fbfaf9;
  }
  #edit-dialog .edit-fields label {
    display: grid;
    align-content: start;
    gap: .4rem;
    min-width: 0;
  }
  #edit-dialog .edit-fields label.wide { grid-column: 1 / -1; }
  #edit-dialog .edit-fields label span {
    color: #5d534e;
    font-size: .74rem;
    font-weight: 800;
  }
  #edit-dialog .edit-fields input,
  #edit-dialog .edit-fields select,
  #edit-dialog .edit-fields textarea {
    width: 100%;
    min-height: 2.7rem;
    box-sizing: border-box;
    padding: .72rem .8rem;
    border: 1px solid #d7cec8;
    border-radius: 3px;
    background: #fff;
    font: inherit;
    color: #302a27;
  }
  #edit-dialog .edit-fields textarea {
    min-height: 6rem;
    resize: vertical;
  }
  #edit-dialog .status {
    min-height: 1.2rem;
    margin: 0;
    padding: .75rem 1.3rem 0;
    color: #7d2939;
  }
  #edit-dialog form > footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-top: 1rem;
    padding: 1rem 1.3rem;
    border-top: 1px solid #e6dfda;
    background: #fff;
  }
  #edit-dialog .primary,
  #edit-dialog .secondary {
    min-height: 2.45rem;
    padding: .65rem .95rem;
    font: inherit;
    font-weight: 750;
    cursor: pointer;
  }
  #edit-dialog .primary {
    border: 1px solid #6f1d2b;
    background: #6f1d2b;
    color: #fff;
  }
  #edit-dialog .secondary {
    border: 1px solid #cbc1ba;
    background: #fff;
    color: #3f3935;
  }

  #detail.detail.card {
    max-width: 900px;
    overflow: hidden;
    border: 1px solid #dfd8d2;
    border-radius: 6px;
    background: #fff;
    box-shadow: none;
  }
  #detail .detail-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
    padding: 1.25rem 1.35rem;
    border-bottom: 1px solid #e5dfda;
    background: #fff;
  }
  #detail .detail-head h2 {
    margin: .2rem 0 .35rem;
    color: #6f1d2b;
    font-size: clamp(1.5rem, 2.5vw, 2rem);
  }
  #detail .detail-head p { margin: 0; color: #71665f; }
  #detail .head-actions { display: flex; align-items: center; gap: .65rem; }
  #detail .detail-sections {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
    padding: 1.25rem 1.35rem;
    border-bottom: 0;
    background: #fbfaf9;
  }
  #detail .detail-group,
  #detail .detail-group:nth-child(even) {
    min-width: 0;
    padding: 1rem;
    border: 1px solid #ded6d0;
    border-radius: 5px;
    background: #fff;
  }
  #detail .detail-group:last-child:nth-child(odd) { grid-column: 1 / -1; }
  #detail .detail-group h3 {
    margin: 0 0 .85rem;
    padding-bottom: .55rem;
    border-bottom: 1px solid #eee7e2;
    color: #6f1d2b;
    font-size: .92rem;
  }
  #detail .detail-group dl {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: .8rem;
    margin: 0;
  }
  #detail .detail-group dl div {
    display: grid;
    grid-template-columns: 1fr;
    gap: .35rem;
    min-width: 0;
  }
  #detail .detail-group dl div:only-child { grid-column: 1 / -1; }
  #detail .detail-group dt,
  #detail .technical dt {
    color: #5d534e;
    font-size: .74rem;
    font-weight: 800;
  }
  #detail .detail-group dd {
    min-height: 2.45rem;
    box-sizing: border-box;
    margin: 0;
    padding: .7rem .78rem;
    border: 1px solid #d7cec8;
    border-radius: 3px;
    background: #fff;
    color: #302a27;
    font-weight: 500;
    line-height: 1.35;
    overflow-wrap: anywhere;
  }
  #detail .technical {
    margin: 0 1.35rem 1.15rem;
    border: 1px solid #e7e0db;
    border-radius: 4px;
    background: #fbf9f7;
  }
  #detail .technical summary {
    padding: .8rem 1rem;
    cursor: pointer;
    font-weight: 750;
    color: #635953;
  }
  #detail .technical > div { padding: 0 1rem 1rem; }
  #detail > footer {
    display: flex;
    justify-content: flex-end;
    padding: 1rem 1.35rem 1.25rem;
    border-top: 1px solid #eee7e2;
    background: #fff;
  }

  @media (max-width: 900px) {
    #edit-dialog .edit-fields,
    #detail .detail-sections,
    #detail .detail-group dl { grid-template-columns: 1fr; }
    #edit-dialog .edit-fields label.wide,
    #detail .detail-group:last-child:nth-child(odd),
    #detail .detail-group dl div:only-child { grid-column: auto; }
    #detail.detail.card { max-width: none; }
    #detail .detail-head { display: grid; }
  }
</style>
