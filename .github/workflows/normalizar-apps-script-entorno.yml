name: Normalización Apps Script (histórica)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  obsolete:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Este fluxo foi puntual e xa está aplicado. Non modifica código."
